    var usuario = prompt("indique las horas extras");
    var cantidasHoras = usuario;
    var CantidadEmpleados = 3;
    var horasReglamentadas = 40
    var salarioBasico = valorHora * horaReglamentadas;


   
  for (i = 1 ; i <CantidadEmpleados ; i++) {

  	if (cantidadHoras > horaReglamentadas) {
  		horasExtras = cantidadHoras - horaReglamentarias;
  		prompt("sus horas extras son: " + horasExtras);

  		valorHoraExtra = ((horasExtras * valorHora) * 0.5) + (horasExtras * valorHora);
  		SalarioPagar = valorHoraExtra + salarioBasico;
  		prompt("su salario con horas extras son" + salariPagar );

  		prompt("Horas extras laboradas" + horaExtra);
  	}
  	
  }
  else 



